import type { Metadata } from "next"; import { PageHero } from "@/components/shared/page-hero"; import { COMPANY, CREDENTIALS } from "@/lib/data/company"; import { pageMetadata } from "@/lib/metadata"; import { Card } from "@/components/ui/card"; import { ShieldCheck } from "lucide-react";
export function generateMetadata():Metadata{return pageMetadata("Experience & Authority","Review Zewar Transport company status, operating history, MC/USDOT placeholders, compliance focus, and authority notes.","/experience-authority")}
export default function Experience(){return <><PageHero eyebrow="Trust & authority" title="Compliance information should be visible—not hidden." description="The current build surfaces the company identifiers prominently while keeping any unverified certificate detail marked for client confirmation."/><section className="section"><div className="container-site grid gap-6 md:grid-cols-2"><Card className="p-7"><ShieldCheck className="h-8 w-8 text-gold-500"/><h2 className="mt-4 text-2xl font-bold">Carrier identifiers</h2><dl className="mt-5 space-y-4"><div><dt className="text-sm text-steel-600">MC number</dt><dd className="font-bold">{COMPANY.mcNumber}</dd></div><div><dt className="text-sm text-steel-600">USDOT number</dt><dd className="font-bold">{COMPANY.dotNumber}</dd></div><div><dt className="text-sm text-steel-600">Insurance certificate</dt><dd className="font-bold">TODO: client to supply certificate details</dd></div></dl></Card><div className="grid gap-4">{CREDENTIALS.map(c=><Card key={c} className="p-5 font-semibold">{c}</Card>)}</div></div></section></>}
