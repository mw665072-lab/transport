import type { Metadata } from "next"; import Link from "next/link"; import { PageHero } from "@/components/shared/page-hero"; import { BLOG_POSTS } from "@/lib/data/blog"; import { pageMetadata } from "@/lib/metadata"; import { Card } from "@/components/ui/card";
export function generateMetadata():Metadata{return pageMetadata("Transportation Blog","Practical road freight articles from Zewar Transport about equipment choice, shipping preparation, and transportation basics.","/blog")}
export default function Blog(){return <><PageHero eyebrow="Blog" title="Practical road-freight guidance." description="Educational content based on the services Zewar Transport actually offers."/><section className="section"><div className="container-site grid gap-6 md:grid-cols-2">{BLOG_POSTS.map(p=><Card key={p.slug} className="p-7"><p className="text-sm text-steel-600">{p.date}</p><h2 className="mt-2 text-2xl font-bold">{p.title}</h2><p className="mt-3 text-steel-600">{p.excerpt}</p><Link className="mt-5 inline-block font-semibold underline decoration-gold-500 decoration-2 underline-offset-4" href={`/blog/${p.slug}`}>Read article</Link></Card>)}</div></section></>}
