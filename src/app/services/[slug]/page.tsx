import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link"; import { notFound } from "next/navigation"; import { CheckCircle2 } from "lucide-react"; import { SERVICES, serviceBySlug } from "@/lib/data/services"; import { pageMetadata } from "@/lib/metadata"; import { PageHero } from "@/components/shared/page-hero"; import { Button } from "@/components/ui/button"; import { JsonLd } from "@/components/shared/json-ld"; import { COMPANY } from "@/lib/data/company";
export function generateStaticParams(){return SERVICES.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=serviceBySlug(slug);return s?pageMetadata(s.name,s.short,`/services/${s.slug}`):{}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=serviceBySlug(slug);if(!s)notFound();const schema={"@context":"https://schema.org","@type":"Service",name:s.name,provider:{"@type":"MovingCompany",name:COMPANY.legalName},areaServed:"United States road lanes within stated coverage",description:s.short};const crumbs={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:COMPANY.domain},{"@type":"ListItem",position:2,name:"Services",item:`${COMPANY.domain}/services`},{"@type":"ListItem",position:3,name:s.name,item:`${COMPANY.domain}/services/${s.slug}`}]};return <><JsonLd data={[schema,crumbs]}/><PageHero eyebrow="Road freight service" title={s.name} description={s.short}/><section className="section"><div className="container-site grid gap-10 lg:grid-cols-[1.1fr_.9fr]"><div><h2 className="text-3xl font-bold text-navy-900">Service overview</h2><p className="mt-5 max-w-[70ch] leading-8 text-steel-600">{s.body}</p><h2 className="mt-10 text-2xl font-bold text-navy-900">Typical load types</h2><div className="mt-5 space-y-3">{s.typicalLoads.map(x=><p key={x} className="flex gap-3"><CheckCircle2 className="mt-0.5 h-5 w-5 shrink-0 text-gold-500"/>{x}</p>)}</div><p className="mt-6 rounded-xl bg-slate-50 p-5 text-sm leading-relaxed text-steel-600"><strong className="text-navy-900">Turnaround:</strong> {s.turnaround}</p><Button asChild className="mt-7"><Link href="/freight-quote">Request a Quote</Link></Button></div><div className="relative aspect-[4/3] overflow-hidden rounded-2xl bg-slate-50"><Image src={s.image} alt={`${s.name} road transportation illustration`} fill sizes="(max-width:1024px) 100vw, 42vw" className="object-cover"/></div></div></section></>}
