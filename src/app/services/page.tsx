import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link"; import { ArrowRight } from "lucide-react"; import { PageHero } from "@/components/shared/page-hero"; import { pageMetadata } from "@/lib/metadata"; import { SERVICES } from "@/lib/data/services"; import { Card } from "@/components/ui/card";
export function generateMetadata():Metadata{return pageMetadata("Road Freight Services","Explore Zewar Transport box truck, hotshot, cargo van, Sprinter van, and road freight transportation services.","/services")}
export default function Services(){return <><PageHero eyebrow="Services" title="The equipment and road services Zewar actually operates." description="No air freight, ocean freight, ports, warehousing claims, or fabricated logistics services—just practical road transportation built around vans, box trucks, and hotshot capacity."/><section className="section"><div className="container-site grid gap-6 md:grid-cols-2">{SERVICES.map(s=><Card key={s.slug} className="overflow-hidden"><div className="relative aspect-[16/9] bg-slate-50"><Image src={s.image} alt={`${s.name} illustration`} fill sizes="(max-width:768px) 100vw, 50vw" className="object-cover"/></div><div className="p-6"><h2 className="text-2xl font-bold text-navy-900">{s.name}</h2><p className="mt-3 leading-relaxed text-steel-600">{s.short}</p><Link href={`/services/${s.slug}`} className="mt-6 inline-flex items-center gap-2 font-semibold text-navy-900">View service <ArrowRight className="h-4 w-4"/></Link></div></Card>)}</div></section></>}
