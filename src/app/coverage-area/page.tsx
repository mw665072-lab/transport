import type { Metadata } from "next"; import Link from "next/link"; import { MapPin } from "lucide-react"; import { PageHero } from "@/components/shared/page-hero"; import { UsMap } from "@/components/shared/us-map"; import { COVERAGE } from "@/lib/data/coverage"; import { pageMetadata } from "@/lib/metadata"; import { Button } from "@/components/ui/button";
export function generateMetadata():Metadata{return pageMetadata("Freight Coverage Area","See Zewar Transport's stated California, Texas, Nevada, Virginia, and surrounding interstate road-freight coverage.","/coverage-area")}
export default function Coverage(){return <><PageHero eyebrow="Coverage" title="Core service areas with surrounding interstate reach." description={`${COVERAGE.states.join(", ")}. ${COVERAGE.note}. Final lane acceptance depends on equipment and driver availability.`}/><section className="section"><div className="container-site grid gap-10 lg:grid-cols-2"><UsMap/><div><h2 className="text-3xl font-bold text-navy-900">States served</h2><div className="mt-5 grid grid-cols-2 gap-3">{COVERAGE.states.map(s=><div key={s} className="flex items-center gap-2 rounded-xl border border-slate-200 p-4 font-semibold"><MapPin className="h-5 w-5 text-gold-500"/>{s}</div>)}</div><h2 className="mt-10 text-2xl font-bold text-navy-900">Example stated lanes</h2><div className="mt-4 space-y-3 text-steel-600">{COVERAGE.lanes.map(x=><p key={x}>• {x}</p>)}</div><Button asChild className="mt-8"><Link href="/freight-quote">Check Your Lane</Link></Button></div></div></section></>}
